// setTimeout(function, milliseconds, param1, param2, ...)
// funcion nativa, no hace falta importarla
// recibe un callback y lo ejecuta despues de un numero determinado de miliseg
// opera sobre modelo asincronico, no bloqueante 


// setInterval(cb, milliseconds, param1, param2, ...): object 
// - funcion nativa, no hace falta importarla
// - A diferencia de timeOut este se ejecuta una y otra vez, cada vez que, 
// se cumple la cantidad de miliseg indicados
// - Se continuara llamando al callback hasta que se llame a clearInterval()
